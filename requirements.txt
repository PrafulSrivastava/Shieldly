# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
geoalchemy2==0.15.2
shapely==2.0.6

# Pydantic / config
pydantic==2.10.4
pydantic-settings==2.7.0

# Cache / pub-sub
redis==5.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client (for Brevo SMS, Google Maps, Gemini, ElevenLabs)
httpx==0.28.1

# Env
python-dotenv==1.0.1

# SafeCall agent (runs as separate process, shares virtualenv)
elevenlabs>=1.0.0
google-genai>=1.0.0
certifi
# pyaudio is only needed when running the agent locally — see requirements-agent.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
fakeredis==2.26.1
